console.log("hello")
var form = document.getElementsByClassName("new-article-form")[0]
// we need to set the remote back to false each time so the other buttons work as normal
form.dataset.remote = "false"
